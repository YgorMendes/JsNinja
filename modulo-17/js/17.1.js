(function(){
  // Manipular strings
   
  let texto = 'aslk jdlaks jgdla skdglak sdla yg adja ldggyggjp kr eyneurnery9 2y'
  console.log(texto.match(/a/g)); // busca os caracteres entre as barras, e o 'g'
  //depois da barra é uma Flag do 'match', que faz buscar todos os caracteres dentro da
  // barra 'g' de 'global'

  // Flags: g: global, i: ignore case
})();